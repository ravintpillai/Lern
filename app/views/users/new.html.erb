<h1>New User</h1>
	<%= form_for @user do |u| %>
		<fieldset>
			<div class='fields'>
				<%= u.label :username %>
				<%= u.text_field :username %>
			</div>
			<div class='field'>
				<%= u.label :email%>
				<%= u.text_field :email%>
			</div>
		</fieldset>
	<%= u.submit %>
	<% end %>
